APP_PORT=8080
DB_HOST=postgres
DB_PORT=5432
DB_USER=subscriptions
DB_PASSWORD=subscriptions
DB_NAME=subscriptions
DB_SSLMODE=disable

# host port mapped to postgres container (docker-compose only)
POSTGRES_HOST_PORT=5433
